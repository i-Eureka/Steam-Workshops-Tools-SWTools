<Window x:Class="SWTools.WPF.MoreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SWTools.WPF"
        xmlns:controls="clr-namespace:SWTools.WPF.Controls"
        xmlns:viewmodel="clr-namespace:SWTools.ViewModel;assembly=SWTools.ViewModel"
        xmlns:md="clr-namespace:MdViewer;assembly=MdViewer"
        mc:Ignorable="d"
        Title="更多信息" 
        Height="520" Width="700"
        ResizeMode="NoResize"
        Background="{StaticResource BhDefault}" ThemeMode="Light"
        WindowStartupLocation="CenterOwner"  ShowInTaskbar="False"
        RenderOptions.BitmapScalingMode="HighQuality"
        Loaded="Window_Loaded">
    <Window.DataContext>
        <viewmodel:MoreWindow/>
    </Window.DataContext>

    <DockPanel>
        <!--底栏-->
        <DockPanel DockPanel.Dock="Bottom" LastChildFill="False" Background="{StaticResource BhGray}">
            <controls:IconButtonAccent x:Name="BtnOk" Click="BtnOk_Click"
                Text="确定" Icon="&#xE8FB;" DockPanel.Dock="Right" Margin="10 10 20 10"/>
        </DockPanel>
        <Separator DockPanel.Dock="Bottom" BorderBrush="{StaticResource BhBorder}"/>
        <!--标签页-->
        <TabControl Margin="0,8,0,0">
            <TabItem IsSelected="True" Background="{StaticResource BhLight}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Margin="5 0 5 0" MinWidth="60">
                        <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xECAA;" VerticalAlignment="Center" Margin="0 0 10 0"/>
                        <TextBlock Text="关于此项目" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <StackPanel Margin="20 20 20 10">
                    <DockPanel LastChildFill="False">
                        <Image Height="60" Source="Resources/icon.png" />
                        <TextBlock VerticalAlignment="Center" FontSize="30" Foreground="#13387b" Margin="10"
                                    Text="Steam Workshop Tools"/>
                        <controls:IconButton x:Name="BtnGithub" Click="BtnGithub_Click"
                                Text="查看 Github 仓库" Icon="&#xE71B;" DockPanel.Dock="Right" Margin="10 10 0 10"/>
                    </DockPanel>
                    <TextBlock Margin="10 20 10 10">
                        <Run Text="{Binding Version, StringFormat=这是 Steam Workshop Tools v{0}。}"/><LineBreak/>
                        <Run Text="{Binding PubVersion, StringFormat=公有账户池版本：{0}}"/>
                    </TextBlock>
                    <TextBlock Margin="10">
                        Copyright © 2025 
                        <Hyperlink NavigateUri="https://github.com/King-zzk/" RequestNavigate="Hyperlink_RequestNavigate">
                            Commkom (king-zzk)
                        </Hyperlink>, <Hyperlink NavigateUri="https://github.com/masterLazy/" RequestNavigate="Hyperlink_RequestNavigate">
                        masterLazy (mLazy)
                        </Hyperlink>。<LineBreak/>
                        <LineBreak/>
                        本项目与 Valve Corporation 没有从属关系。<LineBreak/>
                        这是一个免费、开源的 Steam Workshop 下载工具，请勿倒卖！<LineBreak/>
                        <LineBreak/>
                        根据 GPL-2.0 许可证授权。<LineBreak/>
                        您可以在 “许可证” 标签页查看完整的许可证信息。<LineBreak/>
                        <LineBreak/>
                    </TextBlock>
                </StackPanel>
            </TabItem>
            <TabItem Background="{StaticResource BhLight}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Margin="5 0 5 0" MinWidth="80">
                        <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE713;" VerticalAlignment="Center" Margin="0 0 10 0"/>
                        <TextBlock Text="配置" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <DockPanel Margin="20 10 20 10">
                    <DockPanel DockPanel.Dock="Top" Margin="0 0 0 5">
                        <controls:IconButton x:Name="BtnReset" Click="BtnReset_Click"
                            Text="重置所有设置" Icon="&#xE72C;" DockPanel.Dock="Right"/>
                        <TextBlock VerticalAlignment="Center"
                                Text="您做的大多数更改会被立即保存并生效。"/>
                    </DockPanel>
                    <TreeView Padding="5" AllowDrop="True" ScrollViewer.CanContentScroll="True"
                              Background="{StaticResource BhLight}">
                        <!--常规选项-->
                        <TreeViewItem IsExpanded="True">
                            <TreeViewItem.Header>
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE713;" VerticalAlignment="Center" Margin="0 0 10 0"/>
                                    <TextBlock Text="常规" VerticalAlignment="Center"/>
                                </StackPanel>
                            </TreeViewItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox Content="忽略丢失的文件" IsChecked="{Binding Config.IgnoreMissingFiles}" Margin="0 0 10 0"/>
                                <Button Content="打开总下载目录" x:Name="BtnOpenDownloadFolder" Click="BtnOpenDownloadFolder_Click"/>
                            </StackPanel>
                            <CheckBox Content="禁用自动拉取（包括新发行版检查、账户池更新、公告）" IsChecked="{Binding Config.NoAutoFetch}" Margin="0 0 10 0"/>
                        </TreeViewItem>
                        <!--调试选项-->
                        <TreeViewItem IsExpanded="True">
                            <TreeViewItem.Header>
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xF157;" VerticalAlignment="Center" Margin="0 0 10 0"/>
                                    <TextBlock Text="调试选项" VerticalAlignment="Center"/>
                                </StackPanel>
                            </TreeViewItem.Header>
                            <CheckBox Content="输出调试日志" IsChecked="{Binding Config.LogDebug}"/>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox Content="禁用缓存" IsChecked="{Binding Config.NoCache}" Margin="0 0 10 0"/>
                                <Button Content="清空缓存" x:Name="BtnClearCache" Click="BtnClearCache_Click"/>
                            </StackPanel>
                        </TreeViewItem>
                        <TreeViewItem IsExpanded="True">
                            <TreeViewItem.Header>
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE74C;" VerticalAlignment="Center" Margin="0 0 10 0"/>
                                    <TextBlock Text="Steamcmd" VerticalAlignment="Center"/>
                                </StackPanel>
                            </TreeViewItem.Header>
                            <Button Content="卸载 Steamcmd" x:Name="BtnUninstallSteamcmd" Click="BtnUninstallSteamcmd_Click"/>
                        </TreeViewItem>
                    </TreeView>
                </DockPanel>
            </TabItem>
            <TabItem Background="{StaticResource BhLight}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Margin="5 0 5 0" MinWidth="80">
                        <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE8A5;" VerticalAlignment="Center" Margin="0 0 10 0"/>
                        <TextBlock Text="许可证" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <TextBox Margin="20 10 20 20" IsReadOnly="True" x:Name="LicenseText" Padding="40 10 40 10"
                         FontFamily="Consolas" FontSize="13" VerticalScrollBarVisibility="Visible"/>
            </TabItem>
            <!--Steam 账号-->
            <TabItem Background="{StaticResource BhLight}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Margin="5 0 5 0" MinWidth="80">
                        <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE77B;" VerticalAlignment="Center" Margin="0 0 10 0"/>
                        <TextBlock Text="Steam 账号" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <DockPanel Margin="20 15 20 10">
                    <!--登录状态栏-->
                    <DockPanel DockPanel.Dock="Top" Margin="0 0 0 10" LastChildFill="False">
                        <TextBlock VerticalAlignment="Center" Margin="0 0 8 0"
                                   Text="登录状态："/>
                        <TextBlock x:Name="TxtLoginState" VerticalAlignment="Center"
                                   Text="{Binding LoginStateText}" FontWeight="SemiBold"/>
                    </DockPanel>
                    <!--账号输入区-->
                    <Grid DockPanel.Dock="Top">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!--用户名-->
                        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="用户名"/>
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtUsername"
                                 Text="{Binding Config.CustomUsername, UpdateSourceTrigger=PropertyChanged}"/>
                        <!--密码-->
                        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="密码"/>
                        <PasswordBox Grid.Row="2" Grid.Column="1" x:Name="PbPassword"
                                     PasswordChanged="PbPassword_PasswordChanged"/>
                        <!--Steam Guard 验证码（非必填）-->
                        <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="验证码" Foreground="Gray"/>
                        <TextBox Grid.Row="4" Grid.Column="1" x:Name="TxtGuardCode"
                                 Text="{Binding Config.CustomGuardCode, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Steam Guard 验证码（可选）：填写后将直接使用此验证码登录，留空则弹窗交互"/>
                        <!--记住密码-->
                        <CheckBox Grid.Row="6" Grid.Column="1"
                                  Content="记住密码（明文保存到本地配置）"
                                  IsChecked="{Binding Config.CustomRememberPassword}"
                                  Unchecked="ChkRememberPassword_Unchecked"/>
                        <!--记住验证码-->
                        <CheckBox Grid.Row="8" Grid.Column="1"
                                  Content="记住验证码（明文保存到本地配置）"
                                  IsChecked="{Binding Config.CustomRememberGuardCode}"
                                  Unchecked="ChkRememberGuardCode_Unchecked"/>
                        <!--按钮区-->
                        <StackPanel Grid.Row="10" Grid.Column="1" Orientation="Horizontal">
                            <Button x:Name="BtnLogin" Content="登录 / 重新登录"
                                    IsEnabled="{Binding IsLoginBtnEnabled}"
                                    Click="BtnLogin_Click" Margin="0 0 10 0"/>
                            <Button x:Name="BtnResetLoginState" Content="重置登录状态"
                                    Click="BtnResetLoginState_Click"/>
                        </StackPanel>
                    </Grid>
                    <!--登录日志（可折叠）-->
                    <Expander DockPanel.Dock="Bottom" Header="登录日志" Margin="0 10 0 0"
                              IsExpanded="{Binding IsLoginLogExpanded}">
                        <TextBox x:Name="TxtLoginLog" IsReadOnly="True" Height="100"
                                 FontFamily="Consolas" FontSize="11"
                                 VerticalScrollBarVisibility="Visible"
                                 HorizontalScrollBarVisibility="Auto"
                                 Text="{Binding LoginLog, Mode=OneWay}"
                                 Background="#F5F5F5"/>
                    </Expander>
                    <!--说明文字-->
                    <TextBlock DockPanel.Dock="Bottom" Margin="0 12 0 0" Foreground="Gray"
                               TextWrapping="Wrap"
                               Text="说明：登录成功后，Steamcmd 会在本地缓存会话，后续下载将优先使用此账号。密码仅在&quot;记住密码&quot;开启时才会持久化存储。"/>
                </DockPanel>
            </TabItem>
            <TabItem Background="{StaticResource BhLight}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Margin="5 0 5 0" MinWidth="80">
                        <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE8F1;" VerticalAlignment="Center" Margin="0 0 10 0"/>
                        <TextBlock Text="第三方库" VerticalAlignment="Center"/>
                    </StackPanel>
                </TabItem.Header>
                <md:Markdown x:Name="MdViewer" FontSize="15" />
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
